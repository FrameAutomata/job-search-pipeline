# Local web UI (pipeline/app) — optional, only needed for `run-ui`.
# Install with: pip install -r requirements-ui.txt
fastapi
uvicorn[standard]
markdown
python-multipart  # multipart/form-data parsing for onboarding resume upload
