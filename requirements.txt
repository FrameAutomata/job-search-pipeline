# Pinned because pipeline/sites.py's MUTEX_GROUPS is a claim about this
# version's filter builders. Read the note there, and run
# tests/test_jobspy_contract.py, before bumping.
#
# Do bump it, though — this is the one dependency whose value IS tracking board
# changes, so a stale pin eventually shows up as a scrape returning zero rows,
# which an empty jobs.csv makes indistinguishable from a rate-limit. Nothing
# here initiates that: check `pip index versions python-jobspy` periodically, or
# add a Dependabot pip entry. The contract test is what makes the bump safe, not
# what makes it happen.
python-jobspy==1.1.82
pdfplumber          # PDF resume text extraction (scoring)
odfpy               # ODT resume text extraction (scoring); python-docx (below) handles DOCX
pyyaml
python-dotenv
yake
plyer
anthropic

# LLM providers for --evaluate-batch (both installed by default — small, pure-Python):
google-genai                       # Gemini (free tier available; default cloud provider)
openai                             # OpenAI, Groq, DeepInfra, and Ollama (all OpenAI-compatible)

# DOCX text extraction for resume import (scoring) AND per-job tailored resumes
# for the browser-agent work-order (slot-edits a copy of resumes/resume.docx so
# the formatting stays exactly yours). Pure-Python and tiny, so installed by
# default — tailoring is on whenever resume.docx exists. One-page verification
# additionally uses LibreOffice when installed (optional system dependency;
# without it the budget-guarded .docx is used instead).
python-docx
